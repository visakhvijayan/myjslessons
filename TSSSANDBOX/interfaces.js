/*
function showDevice(device: { id: number; name: string }) {
  console.log(device);
}

let myDevice = { id: 123, name: "Visakh's device" };
showDevice(myDevice);
*/
function showDevice(device) {
    console.log(device);
}
var myDevice = { id: 123, name: "Visakh's device" };
showDevice(myDevice);
